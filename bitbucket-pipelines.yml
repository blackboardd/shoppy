image: node:16

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - node
            script:
              - npm install
              - npm test
        - step:
            name: Code linting
            script:
              - npm run lint
            caches:
              - node
        - step:
            name: Code formatting
            script:
              - npm run format
            caches:
              - node
        - step:
            name: Build style dictionary
            script:
              - npm run build-dictionary
            caches:
              - node
